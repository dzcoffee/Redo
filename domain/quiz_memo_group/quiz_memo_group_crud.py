from domain.quiz_memo_group.quiz_memo_group_schema import MemoQuiz_gCreate
from models import MemoQuiz_group
from sqlalchemy.orm import Session

async def get_quizgroup_by_quizid(db: Session, quizid:int):
    # 값 입력 할것

    return

async def create_problem_group(db: Session, group_create : MemoQuiz_gCreate):
    # 값 입력 할것

    return
