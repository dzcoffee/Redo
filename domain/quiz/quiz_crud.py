from domain.quiz.quiz_schema import QuizCreate
from models import Quiz
from sqlalchemy.orm import Session

async def get_quiz(db: Session, id: int): #비동기식 처리로 수정한 것

    # 값 입력할 것
        
    return 

async def create_quiz(db: Session, quiz: QuizCreate):
    
     # 값 입력할 것

    return 